<UserControl x:Class="Test1.UI.AddCourse"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Canvas>
        <!-- Section -->
        <!-- Update -->
        <Button Canvas.Top="75" Canvas.Left="10" >
            Update
        </Button>

        <!-- Formulaire -->
        <Label Canvas.Left="238" Canvas.Top="295" FontSize="30" Foreground="Azure">
            Courses Name *
        </Label>
        <TextBox Height="42" Canvas.Left="513" TextWrapping="Wrap" Canvas.Top="303" Width="456" Name="coursName" FontSize="20"/>

        <Button Name="envoyer" Canvas.Top="489" Canvas.Left="768" Height="66" Width="201" Click="createCoursesSubmit" FontSize="20" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveCaptionTextBrushKey}}">
            <Button.Effect>
                <BlurEffect/>
            </Button.Effect>
            GO
        </Button>
    </Canvas>
</UserControl>
